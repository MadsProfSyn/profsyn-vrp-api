# Profsyn VRP API

Vehicle Routing Problem solver for inspection scheduling.

## Endpoints

- `GET /` - API status
- `GET /health` - Health check
- `POST /schedule` - Schedule inspections

## Deploy to Railway

1. Push to GitHub
2. Connect Railway to this repo
3. Add environment variables in Railway dashboard